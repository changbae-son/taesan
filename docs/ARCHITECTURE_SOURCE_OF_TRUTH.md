# 근본 재설계 — 3대 정본(Source of Truth) 고정으로 "수정 없는 관리"

작성: 2026-06-16 / 사용자 확정 방향

## 0. 목표
단편적 수정의 악순환("고치면 다른 게 깨진다")을 끝내고, **키움 재수집·reconcile을
다시 돌려도 값이 안 바뀌는** 구조를 만든다. → 앞으로 수정 없이 관리.

## 1. 재발의 진짜 원인 (한 줄)
> 같은 값을 **여러 곳에서 만들고(다중 작성자)**, **매번 다시 만들고(재계산)**,
> **추정으로 만든다(자동 분류)**.

증거(이번 세션 사례):
- 평단 수정 → positions 평단 불일치 (다중 작성자)
- 매도 dedup 수정 → kt00007 중복 (수집 소스 불일치)
- 슬롯 자동분류가 다른 가격 매도 합산 (추정 강제) — 앱클론 +5%에 6/15·6/16 합쳐 18주
- 룰B 설정해도 buyPlans 안 바뀜 (재계산 트리거 누락) — 네이블

## 2. 해결 원칙 (한 줄)
> 값을 **한 곳에서만 만들고(단일 작성자)**, **한 번 확정하면 안 만들고(불변)**,
> 사실은 **키움/사용자 확정으로만 정한다(추정 금지)**.

## 3. 3대 정본 (각 계층의 유일 소스)

| 계층 | 정본(유일 소스) | 규칙 |
|---|---|---|
| ① 체결 사실 | 키움 **주문번호(ord_no)** = 주문체결상세 **kt00007** | 가격·수량·신용 = 키움 그대로. 한 번 저장 = **불변**(재수집해도 안 덮음) |
| ② 잔고·평단 | 키움 잔고 API(kt00005 buy_uv) | 시스템 재계산 **금지** (이미 단일진실 재설계로 적용됨) |
| ③ 분류(슬롯) | **사용자 확정** | 자동(수익률 밴드)은 빈 칸 **제안만**. 충돌 시 미분류. **확정 후 잠금**(reconcile 불변) |

- **파생(잔고표시·슬롯집계)은 저장하지 않음** — 항상 위 3개에서 계산만. (저장하면 작성자가 또 늘어남)

## 4. 왜 재발이 멈추는가
- **체결**: ord_no가 키움 고유 → 같은 가격·수량도 충돌 0, 단가 정확.
  누락(현대약품 168)·중복(앱클론18·시지메드텍198)·단가오류(161214 7850→7800) 원천 차단. 재수집해도 불변.
- **분류**: 사용자 확정 슬롯을 reconcile이 절대 안 바꿈. 자동 합산 모순 소멸. 애매하면 미분류.
- **재계산**: 체결·분류·룰 변경 시 자동 트리거 → "설정했는데 안 바뀜" 소멸.

## 5. 이행 순서 (단계마다 전 종목 연관성 검증 게이트)
1. **체결 정본화** — kt00007(ord_no) 정본 + 기존 fallback 1회 마이그레이션(**분류 승계** 필수).
   → 누락·중복·단가 원천 차단. ※ 마이그레이션 없이 단순 적용하면 기존 fallback과 중복(이번에 확인).
2. **분류 잠금** — 사용자 확정 슬롯 보존 + 같은 라운드+슬롯 충돌(다른 날/가격/ord_no) 시 미분류.
   ※ 부분체결(같은 날·가격·주문)은 합산 유지(1회 매도).
3. **자동 재계산 트리거** — 룰B 설정·저점 확정·체결 변경 시 reconcile 자동(네이블 케이스).
4. **상설 검증** — 매 변경마다 "키움 정본 = 시스템" 전 종목 진단 게이트.

## 6. 현재까지 진행 상태 (2026-06-16 세션)

### 적용 완료(커밋됨)
- 잔고·평단 = 키움 신뢰 (단일진실 재설계 2026-06-13, + 평단 정책 커밋)
- dedup 매도 **다른 ord_no 별개 보존**(index.ts ~line 957) — 분할매도/부분체결 누락 방지
- 안전 자동 dedup `dedupSellFallback`(통합=부분합 & fallback 전부 미분류일 때만 제거)
- 진단: `diagSellGap?source=kt00007|ka10076`, `diagSellDup`, `diagValuation`, `diagAvgRobust`, `diagWatchlist`, `diagSchedules`
- `manualInjectTrade`에 `tradeIdOverride`(ord_no id로 정밀 주입)

### 임시 조치(데이터, 코드 아님)
- 현대약품: 오늘 매도 422주 수동 보강(0133948·0126193 추가, 161214 단가 7850→7800)
- 시지메드텍: fallback 중복 3건 삭제
- 네이블: reconcileNow로 3차 룰B(5463) 재stamp

### 보류(중복 부작용으로 롤백)
- **kt00007 매도 정본화(현금매도 수집)** — 기존 ka10072 fallback과 중복(앱클론 18주 등).
  백업 `pre-kt7-sell` 복원으로 롤백. **fallback→ord_no 대체 마이그레이션이 선행돼야 함.** (= 5장 1단계)
- index.ts line 878 현금매도 스킵 **유지**(원복) — 마이그레이션 전까지 중복 방지.

## 7. 집에서 이어서 할 다음 작업 (우선순위)

### ✅ 1단계 매도 정본화 — 완료 (2026-06-17)
A안(kt00007 주문 정본 + sellSlotSplit 분류 승계)으로 구현·실행 완료.
- 진단/실행 endpoint 4종 (모두 read 우선, 실행은 execute=true 가드):
  - `diagSellMigrateDryRun` — 1:1 (qty·price) 매칭 미리보기
  - `diagSellNoMatchProbe` — ka10072/kt00007 양쪽 조회로 noMatch 정체 분류
  - `diagSellMergePlan` — **부분합 매칭** 마이그레이션 계획 (replace/merge, split 승계, 가격검증)
  - `migrateSellExecute` — 실제 적용(백업→삭제→정본생성→reconcile), 종목 단위 가능
- 결과: 90일 fallback 매도 131그룹 중 129 자동 정본화(replace 183 + merge 27, fallback 249 소비).
  partialFail 0, priceWarnings 0(가격차 ≤1원). 네이블 다중슬롯(MA120:79+5%:1) split 승계 정확.
- 백업: `trades_backup_premerge`(원본 보존, 롤백 가능).
- 멱등: 정본 docId 이미 있으면 set 생략, fallback 자신이 newDocId면 삭제 제외.

### ⚠️ 수동 처리 대기 (qtyMismatch 2건 — kt00007 현금매도 누락)
- **하림지주 2026-05-14**: kt 50주(신용3건) vs fb 80주. 현금매도 30주(20@12910, 10@13520)가 kt00007에 없음.
- **현대약품 2026-06-16**: kt 422주 vs fb 342주. §6 수동보강 잔재(0161214 단가 7800↔7850 불일치 포함).
  → 둘 다 ka10072엔 있고 kt00007엔 없는 현금매도. 수동 보강 또는 ka10072 ord_no 병행 검토.

### ✅ 2단계 분류 잠금 — 완료 (2026-06-17)
사용자 확정 기준: 같은 (라운드, 슬롯)에서 **다른 날짜 OR 같은날 다른가격** = 별개 매도.
같은날·같은가격(순수 부분체결)만 합산.
- `reconcileStockPlans` 자동태깅 직후 충돌 해소: (date,price) 유니크 2단위↑ → 둘째 단위부터
  `sellSlot="unmapped"`. **slotLocked(사용자 확정)은 충돌 시 우선 유지**, 자동만 미분류.
  다중 split(effSlot null)은 보존.
- `retagSells`: 사용자 변경 시 `slotLocked:true` → 자동 충돌 미분류에서 제외(잠금) = 정본 ③.
- 진단 `diagSlotConflict`(기준: date+price 단위, keep=첫단위/unmapped=나머지) — dry-run 후 적용.
- 검증: 앱클론 R2+5% 6/16 미분류(6/15 유지), 전 49종목 errors=0, 남은 충돌 0그룹.
  dry-run에서 같은날 분할 대량 미분류 부작용 확인 → 사용자가 기준2 명시 선택(엄격).

### ✅ 3단계 자동 재계산 트리거 — 완료 (2026-06-17)
네이블 케이스(룰B 설정·저점 확정했는데 buyPlans 차수별 rule·매수가 안 바뀜) 해결.
- `onStockRuleChange`: Firestore `stocks/{id}` onUpdate 트리거. before/after에서
  rule·bottomPrice·referencePeakPrice·referencePeakDate 변경 감지 시 reconcileStockPlans 자동.
- 무한루프 없음: reconcileUpdate가 이 필드들을 안 바꿈(필드 확인). 검증 — reconcileNow→
  트리거 6회 모두 3~116ms skip, reconcile 재호출 0.
- 실작동: 프론트 룰B 설정/저점 확정 시 자동 reconcile(firebase log onStockRuleChange로 확인).

### ✅ 4단계 상설 검증 게이트 — 완료 (2026-06-17)
- `diagHealthCheck`(수동) + `healthCheckCron`(매일 15:50, 이상 시에만 텔레그램):
  잔고·평단(키움 vs 시스템)·슬롯충돌(date+price 단위) 전 종목 점검.
- 첫 점검 발견: 이상 19건(잔고0/평단6/슬롯13). **게이트가 핵심 회귀를 잡음.**

### ⚠️ 핵심 발견 — 다음 최우선 (4단계가 입증)
**1·2단계는 과거 데이터 1회 마이그레이션일 뿐, sync 경로엔 미반영.**
- 자동동기화(kiwoomAutoSync/kiwoomSync)가 **ka10072 fallback + 부분체결을 매번 재수집**
  → 1단계 merge가 다시 쪼개지고, 새 매도도 fallback로 들어옴 → 2단계 충돌이 매일 재발.
- 평단 6건: sync/reconcile이 키움 평단 대신 매수가중으로 회귀(예: 삼천당 sys311658 vs 키움285033).
- **결론: "수정 없이 관리"의 진짜 관문 = sync 자체를 kt00007 ord_no 정본화 + fallback
  마이그레이션 동반.** 1단계 kt00007 정본화(현재 보류, 중복 부작용)를 마이그레이션 붙여 완성해야.

### 다음 작업 (우선순위)
1. **sync 정본화(1단계 완성)** — kiwoomSync/AutoSync 매도 수집을 kt00007(ord_no) 정본으로 +
   기존/신규 fallback→ord_no 대체(분류 승계). 이게 슬롯충돌·평단 재발의 근본.
2. 평단 회귀 원인(reconcile avgPrice 키움 신뢰 정책) 점검.
3. line 878 현금매도 스킵 제거(정본화 후).
4. 미분류 ~42건 사용자 분류(slotLocked), qtyMismatch 2건 정리.
